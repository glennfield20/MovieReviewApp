= simple_form_for([@movie, @review]) do |f|
  = f.error_notification
  .field
    #star-rating
  .form-inputs
    = f.input :comment
  .form-actions
    = f.button :submit

%script
  $('#star-rating').raty({
  path: '/assets/',
  scoreName: 'review[rating]'
  });
